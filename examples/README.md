# Examples, not live project memory

These files are fixtures for understanding the proposed formats. They were not captured from a real application, verified agent session or approved user decision. Do not install them as actual project guidance.

The proposal refers to the example wiki page and has a real base hash of that fixture file, solely to demonstrate optimistic concurrency. Its provider/model names and source ID are explicitly examples. The event demonstrates an envelope, not a claim that any agent delivered context.

The example AGENTS block and skill are templates for future review. They do not prove any agent-specific installation path or automatic retrieval hook.
