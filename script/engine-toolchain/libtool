#!/bin/bash
set -euo pipefail
# Xcode 27 libtool skips Zig archive members as unaligned. LLVM's Darwin
# archiver preserves them; mise supplies the pinned llvm-tools package.
exec llvm-libtool-darwin "$@"
